application:
  port: 8000
  host: 127.0.0.1
  base_url: "http://127.0.0.1"
  # API KEY to validate in every request, the `Authorization` header must be present and its value start with "Bearer"
  # e.g: {"Authorization": "Bearer tokentest123"}
  api_key: "Bearer test123"
  
database:
  # name of the test database, this database will be droped and created when running the tests
  # for safety, the program will panic if the database name does not contain the "TEST" string.
  test_database_name: "TEST_RUST"
  port: 3306
  username: "test"
  password: "testuserfromrustlangthatimlearning"
  database_name: "coupon"
  require_ssl: false
