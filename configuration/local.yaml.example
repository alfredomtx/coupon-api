application:
  host: 127.0.0.1
  base_url: "http://127.0.0.1"
  # API KEY to validate in every request, must start with the Authorization header must "Bearer"
  api_key: "Bearer test123"

  
database:
  require_ssl: false
